# VTT2SRT-Tool
VTT2SRT-Tool is a simple Windows GUI application that converts WebVTT (.vtt) transcript files into SRT subtitle files. Supports batch conversion, folder processing, optional ffmpeg integration, and an easy-to-use interface.

## Features:

- Converts `.vtt` → `.srt`
- Supports multiple files at once
- Supports folder conversion
- Works with or without ffmpeg
- Automatically opens output folder
- Overwrites existing subtitles
- Removes original `.vtt` files
- Simple GUI

## Screenshot

Add screenshots here later.

## Installation

### Install Python

Download Python:

https://www.python.org/downloads/

### Install Dependencies

```bash
pip install -r requirements.txt
